// Opens 'options.html', when user installs the extension
// chrome.runtime.onInstalled.addListener(() => {
//     chrome.tabs.create({
//         url: 'https://arnavdas.in'
//     })
// })

// Opens 'feedback.html', when user uninstalls the extension